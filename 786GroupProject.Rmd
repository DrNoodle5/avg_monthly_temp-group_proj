---
title: "786GroupProject"
author: "Simon Loh"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
library(imputeTS)
data <- read_csv("wellington.csv") %>%
  mutate(Date = yearmonth(paste(Year, Month))) %>%
  select(-Month, -Year) %>% # removed month and year because we already have date.
  as_tsibble(index = Date)

colSums(is.na(data))

scan_gaps(data)

data <- data %>%
  fill_gaps() %>%
  mutate(Temperature = na_seadec(Temperature, algorithm = "interpolation"))

training <- data %>%
  filter(Date <= yearmonth("2025 April"))

test <- data %>%
  filter(Date > yearmonth("2025 April"))
```


